<div class="form-group">
  <div class="col-sm-10 col-sm-offset-2">
      <input type="submit" class="<?php if (@$className) { echo $className; } else { echo 'btn btn-default'; } ?>" value="<?php echo $value?>" <?php 
        if (is_array(@$otherAttributes)) {
          foreach($otherAttributes as $k=>$v) {
            echo "$k='$v'";
          }
        }
        ?> />
    </div>
</div>